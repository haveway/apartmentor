<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="visitCarMapper">


	<insert id="enrollVisitCar" parameterType="parking">
		INSERT
		  INTO
		  	   PARKING
		  	   (
		  	   CAR_NO,
		  	   CAR_PHONE,
		  	   STATUS,
		  	   CAR_CATEGORY,
		  	   CAR_DATE,
		  	   CAR_PURPOSE,
		  	   CREATE_DATE,
		  	   USER_NO
		  	   )
		VALUES
				(
				#{carNo},
				#{carPhone},
				<choose>
					<when test="carDate == createDate">
						'Y',
					</when>
					<otherwise>
						'N',
					</otherwise>
				</choose>
				#{carCategory},
				#{carDate},
				#{carPurpose},
				#{createDate},
				#{userNo}
				)
	</insert>
	
	<update id="SetDayVisitCar" parameterType="string">
		UPDATE PARKING
	  	   SET STATUS = CASE
	   	  WHEN CAR_DATE = #{today}
	   	  THEN 'Y'
	   	  ELSE 'N'
	       END
	</update>

</mapper>
